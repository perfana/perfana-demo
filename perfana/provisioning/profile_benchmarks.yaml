organizationId: __ORG_ID__  # placeholder; rewritten by bootstrap.sh to the real org id
# No default benchmarks ship for the jmeter profile. Define benchmarks per
# system-under-test / workload from the Perfana UI, or add jmeter items here.
items: []
